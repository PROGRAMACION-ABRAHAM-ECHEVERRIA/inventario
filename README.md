# dependencias 
NEST 
npm install -g @nestjs/cli 

VALIDACION DE DTOS
npm install @nestjs/class-validator --save
npm install class-transformer   
npm install class-validator class-transformer
npm install @nestjs/mapped-types

CONECCION A LA BASE Y ORM  
<!-- Para sqlserver -->
npm install @nestjs/typeorm typeorm mssql 
npm install mssql

DEPENDENCIAS DE AUTENTICACION
token
npm install --save @nestjs/jwt passport-jwt 
npm install --save-dev @types/passport-jwt 

encriptacion de contraseñas  
<!-- no se han instalado -->
npm i --save-dev @types/bcrypt
npm install --save bcrypt 

manejo de horas y fechas 
npm install moment
npm install @types/moment 

SWAGGER 
npm install --save @nestjs/swagger


# version de node js  
20.10.0  

# version de nest cli js 
11.0.4

# funcion para cronjob 
<!-- no se ha instalado -->
npm install @nestjs/schedule 

# paqueteria para hacer peticiones http desde esta api
npm install @nestjs/axios axios 

# manejo de cookies 
npm install cookie-parser